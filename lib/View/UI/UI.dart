import 'package:api_task1/View/UI/Pages/newAlbumList.dart';
import 'package:flutter/material.dart';

class UiListView extends StatelessWidget {
  const UiListView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return NewAlbum();
  }
}
